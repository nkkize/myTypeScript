let firstName = "narender";
let age= 20;

let someVar: string;

function simpleFunc(name: string, isActive: boolean) : number {
    return 1;
}

function simpleFunc2(name: string) : void {
    //nothing returned
}
